{
  "$schema": "https://example.com/manifest-v1",
  "fixtures": [
    {
      "file": "search_one_piece.json",
      "purpose": "Mock GraphQL response for search_anime when query='one+piece'. Three edges: the main One Piece TV series and two films, each with availableEpisodes counts and an airedStart date.",
      "captured_at": "2026-05-06",
      "captured_via": "Synthetic — values match the shape of real allanime API responses; ids are real (visible in the public API).",
      "notes": "Used by tests/bash/network/search_anime.bats. airedStart added 2026-07-20 for ani-cli 4.14.5, whose search selection requests it and whose result sed requires a year field."
    },
    {
      "file": "search_empty.json",
      "purpose": "Mock GraphQL response for a query that matches nothing.",
      "captured_at": "2026-05-06",
      "captured_via": "Synthetic.",
      "notes": "Used by tests/bash/network/search_anime.bats."
    },
    {
      "file": "episodes_attack_on_titan.json",
      "purpose": "Mock GraphQL response for episodes_list with a 25-episode show. Lists are descending by episode number; the script sort -n is responsible for ordering.",
      "captured_at": "2026-05-06",
      "captured_via": "Synthetic.",
      "notes": "Used by tests/bash/network/episodes_list.bats."
    },
    {
      "file": "episodes_short.json",
      "purpose": "Mock GraphQL response for a 4-episode show with a fractional episode (1.5). Dub list is empty so dub mode tests can assert on the empty-list shape.",
      "captured_at": "2026-05-06",
      "captured_via": "Synthetic.",
      "notes": "Used by tests/bash/network/episodes_list.bats."
    },
    {
      "file": "keys_page.html",
      "purpose": "Mock referrer page for fetch_keys (ani-cli 4.15 encrypted transport). Carries the epoch, the base64 partB (bytes 0x00..0x1f), and the CDN app bundle URL.",
      "captured_at": "2026-07-27",
      "captured_via": "Synthetic — shape matches the fields ani-cli's fetch_keys extracts (\"epoch\":N, \"partB\":\"...\", entry/app.*.js URL).",
      "notes": "partB must not be all-identical bytes: ani-cli reads it back through od, which collapses repeated 16-byte lines into a literal '*'. Used by tests/bash/acceptance/cli_search_and_play.bats via the curl shim."
    },
    {
      "file": "keys_app.js",
      "purpose": "Mock CDN app bundle for fetch_keys; names the chunk file the key mask lives in.",
      "captured_at": "2026-07-27",
      "captured_via": "Synthetic — matches the \"../chunks/*.js\" import shape fetch_keys greps for.",
      "notes": "Used by tests/bash/acceptance/cli_search_and_play.bats via the curl shim."
    },
    {
      "file": "keys_chunk.js",
      "purpose": "Mock CDN chunk for fetch_keys; carries the 64-hex key mask (bytes 0x00..0x1f). mask XOR partB derives the all-zero fixture key blob_builder.sh encrypts with.",
      "captured_at": "2026-07-27",
      "captured_via": "Synthetic — the mask is the only 64-hex run in the file, as fetch_keys' grep requires.",
      "notes": "Used by tests/bash/acceptance/cli_search_and_play.bats via the curl shim."
    }
  ]
}
