The working contract for this repository lives in AGENTS.md. It is
imported below rather than merely referenced: Claude Code loads this
file automatically and follows `@path` imports, but it does not follow
a prose pointer, so an English "see AGENTS.md" left those rules out of
context unless someone happened to open the file by hand.

@AGENTS.md
